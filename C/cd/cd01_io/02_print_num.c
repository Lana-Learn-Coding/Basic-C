#include <stdio.h>
#include <conio.h>

int main(int argc, char const *argv[])
{
	int num;
	printf("nhap 1 so nguyen: ");
	scanf("%d", &num);
	printf("so ban vua nhap la: %d\n", num);

	getch();
	return 0;
}
